import AddStage from '@/Components/Admin/Stages/AddStage';

export const metadata = {
  title: "Add Stage - Admin",
  description: "Add new stage",
  keywords: "add, stage, admin",
};

const page = () => {
    return (
        <AddStage />
    )
}

export default page
