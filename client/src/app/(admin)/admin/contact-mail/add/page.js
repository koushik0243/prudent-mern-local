import AddContactMail from '@/Components/Admin/ContactMail/AddContactMail';

export const metadata = {
  title: 'Add Contact Mail - Admin',
  description: 'Add new contact mail record',
  keywords: 'add, contact, mail, admin',
};

const page = () => {
  return <AddContactMail />;
};

export default page;
