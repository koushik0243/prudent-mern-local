import AddContactMailSend from '@/Components/Admin/ContactMailSend/AddContactMailSend';

export const metadata = {
  title: 'Add Contact Mail Send - Admin',
  description: 'Add new contact mail send record',
  keywords: 'add, contact, mail, send, admin',
};

const page = () => {
  return <AddContactMailSend />;
};

export default page;
