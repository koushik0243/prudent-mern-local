import AddState from '@/Components/Admin/State/AddState';

export const metadata = {
  title: 'Add State - Admin',
  description: 'Add new state',
  keywords: 'add, state, admin',
};

const page = () => {
    return (
        <AddState />
    );
};

export default page;
