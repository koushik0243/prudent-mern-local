import AddTagManager from '@/Components/Admin/TagManagers/AddTagManager';

export const metadata = {
  title: "Add Tag Manager - Admin",
  description: "Add new tag manager",
  keywords: "add, tag, manager, admin",
};

const page = () => {
    return (
        <AddTagManager />
    )
}

export default page
